# 🛒 AI Grocery Tracker (PWA)

A portable, offline-first web app that uses **Gemini 3 Flash** to scan receipts and track price history.

## 🚀 How to Start (Portability)

1. **Clone the project:**
   `git clone <your-repo-link>`

2. **Launch the Database (Docker):**
   `docker-compose up -d`

3. **Install Frontend:**
   `cd app && npm install`

4. **Run the App:**
   `npm run dev`

## 🛠️ Stack
- **Frontend:** Next.js (PWA)
- **Database:** Supabase/Postgres (Local Docker)
- **AI:** Google Gemini 3 Flash